# === 核心依赖 ===
requests>=2.31.0
mitmproxy>=10.0.0

# === 语音识别 (faster-whisper GPU 加速) ===
faster-whisper>=1.2.0          # CTranslate2 引擎，比 openai-whisper 快 4-8 倍
# openai-whisper>=20231117     # 旧方案，已弃用
imageio-ffmpeg>=0.4.9          # 内置 ffmpeg 二进制

# === PyTorch (CUDA GPU 支持) ===
# 安装方式 (二选一):
#   GPU:  pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
#   CPU:  pip install torch torchvision torchaudio
# 注意: 不要直接 pip install -r requirements.txt 安装 torch，需手动选择 CUDA 版本
# torch>=2.0.0

# === 大模型 (大纲生成 & ASR 纠错) ===
openai>=1.0.0

# === 云端 ASR (可选，精度高于 Whisper) ===
# alibabacloud-nls>=1.0.0      # 阿里云 ASR
# websocket-client>=1.6.0      # 讯飞 ASR 依赖
